-- Bezz -- content pulled from public BAR replays (bar-replay-miner global scan, 2026-08-15)
-- 23 distinct snippet(s), 1308 total occurrences across scanned games

-- === "-- T3 Coms Constructor Boost by Bezz-- T3 Com Tweaks" (seen 250x) ===
-- T3 Coms Constructor Boost by Bezz-- T3 Com Tweaks
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

local boostedEarlyInfra={}
local boostedScouts={}

-- === "-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz" (seen 294x) ===
-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Commander Corpse / Explosion Normalization
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

-- === "-- T3 Coms Early Infrastructure + Scout Boost by Bezz" (seen 103x) ===
-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Commander Corpse / Explosion Normalization
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

-- === "-- by Bezz" (seen 224x) ===
-- by Bezz

local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

-- === "-- T3 Coms Constructor Boost by Bezz" (seen 85x) ===
-- T3 Coms Constructor Boost by Bezz-- T3 Com Tweaks
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

local boostedEarlyInfra={}
local boostedScouts={}

-- === "-- T3 Coms Early Infrastructure + Scout Boost by Bezz-- T3 Com Cons" (seen 3x) ===
-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Commander Corpse / Explosion Normalization
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

-- === "-- T3 Coms Early Infrastructure + Scout Boost by Bezz-- T3 Com Tweaks2" (seen 180x) ===
-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Commander Corpse / Explosion Normalization
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

-- === "-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Tweaks2" (seen 73x) ===
-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Commander Corpse / Explosion Normalization
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

-- === "-- T3 Com Tweaks9 SAFE ECO + Cross-Faction Mex by Bezz" (seen 1x) ===
-- T3 Com Tweaks9 SAFE ECO + Cross-Faction Mex by Bezz

-- === "-- T3 Com Tweaks9 T3/Epic ECO + AI ECO Hints by Bezz" (seen 1x) ===
-- T3 Com Tweaks9 T3/Epic ECO + AI ECO Hints by Bezz

-- === "-- T3 Com Tweaks9 Epic / T4 ECO Unlocks by Bezz" (seen 1x) ===
-- T3 Com Tweaks9 Epic / T4 ECO Unlocks by Bezz

-- === "-- T3 Com Tweaks9 Ratte + Epic Air Construction Aide by Bezz" (seen 2x) ===
-- T3 Com Tweaks9 Ratte + Epic Air Construction Aide by Bezz

-- === "-- T3 Com Tweaks9 MIN by Bezz" (seen 1x) ===
-- T3 Com Tweaks9 MIN by Bezz

-- === "-- T3 Coms Epic Scav Units Damage Nerf by Bezz" (seen 7x) ===
-- T3 Coms Epic Scav Units Damage Nerf by Bezz

-- === "-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Commander" (seen 15x) ===
-- T3 Coms Early Infrastructure + Scout + Commander Eco Boost by Bezz-- T3 Com Commander Corpse / Explosion Normalization
local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

-- === "-- T3 Coms Commander Corpse/Explosion Boost by Bezz" (seen 3x) ===
-- T3 Coms Commander Corpse/Explosion Boost by Bezz

-- === "-- T3 Coms Commander Corpse/Explosion + Non-Blocking Aim Test by Bezz" (seen 1x) ===
-- T3 Coms Commander Corpse/Explosion + Hull Turn Boost by Bezz

-- === "-- T3 Coms Commander Corpse/Explosion + Hull Turn Boost by Bezz" (seen 3x) ===
-- T3 Coms Commander Corpse/Explosion + Hull Turn Boost by Bezz

-- === "-- T3 Commanders by Bezz-- T3 Com Mod v1.3 B" (seen 1x) ===
-- T3 Commanders by Bezz-- T3 Com Mod v1.3 Bezz-- T3 Com Cons

local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

local function roundValue(value)
    if type(value)~="number" then return value end

    return math.floor(value+0.5)
end

local boostedConstructors={}

-- === "-- T3 Com Mod v1.2 by Bezz-- T3 Com Cons" (seen 1x) ===
-- T3 Com Mod v1.2 by Bezz-- T3 Com Cons

local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

local function roundValue(value)
    if type(value)~="number" then return value end

    return math.floor(value+0.5)
end

local boostedConstructors={}

-- === "-- T3 Coms Constructor + Early Infrastructure + Scout Boost by Bezz" (seen 7x) ===
-- T3 Coms Constructor + Early Infrastructure + Scout Boost by Bezz

-- === "-- T3 Commanders by Bezz" (seen 14x) ===
-- T3 Commanders by Bezz-- T3 Com Mod v1.3 Bezz-- T3 Com Cons

local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

local function roundValue(value)
    if type(value)~="number" then return value end

    return math.floor(value+0.5)
end

local boostedConstructors={}

-- === "-- T3 Commanders by Bezz-- T3 Commanders Mod" (seen 38x) ===
-- T3 Commanders by Bezz-- T3 Commanders Mod v1.1-- T3 Com Cons

local commanderUnits={
    armcom=true,
    corcom=true,
    legcom=true,
}

local function roundValue(value)
    if type(value)~="number" then return value end

    return math.floor(value+0.5)
end

local boostedConstructors={}

